#! /bin/sh
. /etc/default/lejos
# show shutdown splash
cat ${LEJOS_HOME}/bin/images/shutdown.ev3i > /dev/fb
#echo "Set power off flag..."
#cd ${LEJOS_HOME}/bin/utils
#${LEJOS_HOME}/bin/jrun PowerOff 
sync;sync
#Power will be turned off when modules are unloaded

